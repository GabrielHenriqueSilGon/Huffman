#ifndef _COMPARA_HPP
#define _COMPARA_HPP

#include "tree.hpp"

using namespace std;

void Picador(seiva palavra);
void contas(seiva *palavra, string str);
void MinMax(seiva *palavra);
void HuffmanRP (seiva * palavra);

#endif